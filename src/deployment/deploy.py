"""Deploy the registered model to an Azure ML managed online endpoint. (Phase 7)"""

from __future__ import annotations

import typer


def main() -> None:
    """Deploy model to managed endpoint. (Phase 7)"""
    typer.echo("deploy-model is implemented in Phase 7.")


def test() -> None:
    """Send a test query to the deployed endpoint. (Phase 7)"""
    typer.echo("test-endpoint is implemented in Phase 7.")
